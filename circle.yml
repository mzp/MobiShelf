machine:
  ruby:
    version:
      ruby-2.4.0
database:
  override: []
test:
  override:
    - bundle update
deployment:
  auto-bundle-update:
    branch: circleci
    commands:
      - ./bin/circleci/create-pr
